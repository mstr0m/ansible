all:
  children:
    worker:
      hosts:
        172.31.43.82:
        172.31.37.219:
    master:
      hosts:
        localhost:
  vars:
    #ansible_become=yes
    #ansible_ssh_private_key_file = ./aws.pem
